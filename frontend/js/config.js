/* ==========================================================
   Stellar Agri AI - Configuration
   ========================================================== */

const CONFIG = {
    // Direct backend server URL (works with file://, localhost, or 127.0.0.1)
    API_URL: 'http://127.0.0.1:8000/chat',
    
    // Application Info
    APP_NAME: 'Stellar Agri AI'
};
